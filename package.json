{
  "name": "elements-game",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd api && npm run dev\" \"cd admin && npm run dev\"",
    "setup": "cd api && npm install && npm run setup && cd ../admin && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
